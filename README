This repo has analysis code and templates for plotting various kinematic distributions. The input files are .root files generated in the chain madgraph->pythia->delphes.

Here are instructions for committing and pushing locally:
mkdir repoName
cd repoName
git init
(create or copy in file fileName)
git add fileName
git commit -m 'commit description'
git remote add origin https://github.com/username/repoName.git
git push origin master

----------------------------------------------------
Here are instructions for cloning, committing, and pushing remotely:
navigate to https://github.com/dburns7/ResearchRepo and click Fork in the upper right corner
Click on your username
In terminal:
git clone https://github.com/dburns7/ResearchRepo.git
cd ResearchRepo
git remote add upstream https://github.com/dburns7/ResearchRepo.git

To pull changes:
git fetch upstream

To add/update files:
git add FILENAME
git commit -m 'text describing commit'
git push upstream master



